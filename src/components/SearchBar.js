import React, { Fragment } from 'react'
import styled from 'styled-components'
import { Link } from 'react-router-dom'

const SearchBar = ({ value, onChange, placeholder }) => <div />

export default SearchBar
